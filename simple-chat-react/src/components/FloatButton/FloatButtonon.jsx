import React from 'react'
import classes from './FloatButton.module.css'

export default function FloatButton() {
    return (
        <button className={classes.floatButton}>
            <span className="material-icons">create</span>
        </button>
    )
}
